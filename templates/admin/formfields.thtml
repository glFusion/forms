{# begin {templatelocation} #}
<p>{field_adminlist}</p>
<form action="{action_url}" method="POST">
  <input type="hidden" name="frm_id" value="{frm_id}" />
{!if is_uikit}
  <button name="editfield" class="uk-button uk-button-success" type="submit">{$LANG_FORMS['add_field']}</button>
{!else}
  <input name="editfield" class="lgButton green" type="submit" value="{$LANG_FORMS['add_field']}" />
{!endif}
</form>
{# end {templatelocation} #}

